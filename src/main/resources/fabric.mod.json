{
  "schemaVersion": 1,
  "id": "servertraders",
  "version": "${version}",
  "name": "ServerTraders",
  "description": "Adds server-controlled traders, which read their trades from config files and can update on the fly.",
  "authors": ["JongWasTaken"],
  "contact": {},
  "license": "MIT",
  "entrypoints": {
    "main": [
      "dev.smto.servertraders.ServerTraders"
    ]
  },
  "mixins": [
    "servertraders.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}",
    "fabric-permissions-api-v0": "*"
  }
}
